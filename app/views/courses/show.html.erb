<div class="container">
  <h1>single course</h1>
  <% flash.each do |message_type, message| %>
    <div class="alert alert-<%= message_type %>"><%= message %></div>
  <% end %>
  <%= link_to "delete this course ? ...", course_delete_path(id: @course.course_id) %>

  <%= render 'courses/form' %>
</div>
